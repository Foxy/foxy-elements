import dedent from 'dedent';
import { html } from 'lit-html';
import { unsafeHTML } from 'lit-html/directives/unsafe-html';
import {
  Subtitle,
  ArgsTable,
  Story,
  Canvas,
  Meta,
} from '@web/storybook-prebuilt/addon-docs/blocks.js';
import './index.ts';

<Meta
  title="Cards / AddressCard"
  argTypes={{
    hiddencontrols: {
      control: {
        type: 'check',
        options: ['address-name', 'full-name', 'full-address', 'company', 'phone'],
      },
    },
    lang: {
      control: {
        type: 'select',
        options: ['en', 'es'],
      },
    },
  }}
/>

# AddressCard

<Subtitle>Card element displaying a customer address</Subtitle>

<Canvas
  mdxSource={dedent`
    <foxy-address-card
      parent="https://demo.foxycart.com/s/admin/customers/0/customer_addresses"
      href="https://demo.foxycart.com/s/admin/customer_addresses/0"
    >
    </foxy-address-card>
  `}
  isExpanded
>
  <Story
    name="Playground"
    args={{
      hiddencontrols: [],
      lang: 'en',
      href: 'https://demo.foxycart.com/s/admin/customer_addresses/0',
      parent: 'https://demo.foxycart.com/s/admin/customers/0/customer_addresses',
      'address-name:before': '',
      'address-name:after': '',
      'full-name:before': '',
      'full-name:after': '',
      'full-address:before': '',
      'full-address:after': '',
      'company:before': '',
      'company:after': '',
      'phone:before': '',
      'phone:after': '',
    }}
  >
    {args =>
      html`
        <foxy-address-card
          hiddencontrols=${args.hiddencontrols.join(' ')}
          parent=${args.parent}
          class="box-content w-64 p-4 rounded-lg border"
          href=${args.href}
          lang=${args.lang}
        >
          <div slot="address-name:before">${unsafeHTML(args['address-name:before'])}</div>
          <div slot="address-name:after">${unsafeHTML(args['address-name:after'])}</div>
          <div slot="full-name:before">${unsafeHTML(args['full-name:before'])}</div>
          <div slot="full-name:after">${unsafeHTML(args['full-name:after'])}</div>
          <div slot="full-address:before">${unsafeHTML(args['full-address:before'])}</div>
          <div slot="full-address:after">${unsafeHTML(args['full-address:after'])}</div>
          <div slot="company:before">${unsafeHTML(args['company:before'])}</div>
          <div slot="company:after">${unsafeHTML(args['company:after'])}</div>
          <div slot="phone:before">${unsafeHTML(args['phone:before'])}</div>
          <div slot="phone:after">${unsafeHTML(args['phone:after'])}</div>
        </foxy-address-card>
      `
    }
  </Story>
</Canvas>

## Reference

<ArgsTable of="foxy-address-card" />

### hAPI

This element supports the following resources: [customer_address](https://api.foxycart.com/rels/customer_address), [default_billing_address](https://api.foxycart.com/rels/default_billing_address), [default_shipping_address](https://api.foxycart.com/rels/default_shipping_address).

### i18next

This element supports the following namespaces: `address-card`, `shared`.

### Theming

Our elements are built with Vaadin Lumo theme and therefore share the list of CSS Custom Properties with it. You can find the latest documentation and theme editor on [demo.vaadin.com](https://demo.vaadin.com/lumo-editor/).

<Story name="State: idle.snapshot" parameters={{ docs: { disable: true } }}>
  {() =>
    html`
      <foxy-address-card
        parent="https://demo.foxycart.com/s/admin/customers/0/customer_addresses"
        href="https://demo.foxycart.com/s/admin/customer_addresses/0"
        class="box-content w-64 p-4 rounded-lg border"
      >
      </foxy-address-card>
    `
  }
</Story>

<Story name="State: idle.template" parameters={{ docs: { disable: true } }}>
  {() =>
    html`
      <foxy-address-card
        parent="https://demo.foxycart.com/s/admin/customers/0/customer_addresses"
        class="box-content w-64 p-4 rounded-lg border"
      >
      </foxy-address-card>
    `
  }
</Story>

<Story name="State: busy" parameters={{ docs: { disable: true } }}>
  {() =>
    html`
      <foxy-address-card
        parent="https://demo.foxycart.com/s/admin/customers/0/customer_addresses"
        href="https://demo.foxycart.com/s/admin/sleep"
        class="box-content w-64 p-4 rounded-lg border"
      >
      </foxy-address-card>
    `
  }
</Story>

<Story name="State: fail" parameters={{ docs: { disable: true } }}>
  {() =>
    html`
      <foxy-address-card
        parent="https://demo.foxycart.com/s/admin/customers/0/customer_addresses"
        href="https://demo.foxycart.com/s/admin/not-found"
        class="box-content w-64 p-4 rounded-lg border"
      >
      </foxy-address-card>
    `
  }
</Story>
