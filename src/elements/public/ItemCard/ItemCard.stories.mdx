import { html } from 'lit-html';
import { Story, Canvas, Meta } from '@storybook/addon-docs/blocks';
import { item } from '../../../mocks/FxItem';
import './index.ts';

<Meta title="Cards / Item" component="foxy-item-card" />

# Item

[![hAPI](https://img.shields.io/badge/hAPI-item-green.svg)](https://api.foxycart.com/rels/item)

## States

Below are all the states this component can render.

### Loading

Displayed while loading the resource.

export function handleRequestWithLoading(evt) {
  evt.detail.handle(() => new Promise(() => {}));
}

<Canvas>
  <Story name="Loading">
    {() =>
      html`
        <foxy-item-card
          class="mx-auto"
          style="width: 240px"
          href="https://api.foxy.test/items/5616394"
          @request=${handleRequestWithLoading}
        >
        </foxy-item-card>
      `
    }
  </Story>
</Canvas>

### Error

Displayed when resource load fails.

export function handleRequestWithError(evt) {
  evt.detail.handle(() => Promise.reject());
}

<Canvas>
  <Story name="Error">
    {() =>
      html`
        <foxy-item-card
          class="mx-auto"
          style="width: 240px"
          href="https://api.foxy.test/items/5616394"
          @request=${handleRequestWithError}
        >
        </foxy-item-card>
      `
    }
  </Story>
</Canvas>

### Ready

Displayed after the resource is loaded.

export function handleRequestWithSuccess(evt) {
  evt.detail.handle(async () => new Response(JSON.stringify(item)));
}

<Canvas>
  <Story name="Ready">
    {() =>
      html`
        <foxy-item-card
          class="mx-auto"
          style="width: 240px"
          href="https://api.foxy.test/items/5616394"
          @request=${handleRequestWithSuccess}
        >
        </foxy-item-card>
      `
    }
  </Story>
</Canvas>
