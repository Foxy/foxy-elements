import { html } from 'lit-html';
import { Story, Canvas, Meta } from '@storybook/addon-docs/blocks';
import './index.ts';

<Meta title="Forms / Subscription" component="foxy-subscription-form" />

# Subscription Form

[![hAPI](https://img.shields.io/badge/hAPI-subscription-green.svg)](https://api.foxycart.com/rels/subscription)

## States

Below are all the states this component can render.

### Loading

Displayed while loading the resource.

<Canvas>
  <Story name="Loading">
    {() =>
      html`
        <foxy-subscription-form
          class="max-w-xl mx-auto"
          href="https://demo.foxycart.com/s/admin/sleep"
        >
        </foxy-subscription-form>
      `
    }
  </Story>
</Canvas>

### Error

Displayed when resource load fails.

<Canvas>
  <Story name="Error">
    {() =>
      html`
        <foxy-subscription-form
          href="https://demo.foxycart.com/s/admin/not-found"
          class="max-w-xl mx-auto"
        >
        </foxy-subscription-form>
      `
    }
  </Story>
</Canvas>

### Ready

Displayed after the resource is loaded.

<Canvas>
  <Story name="Ready">
    {() =>
      html`
        <foxy-subscription-form
          href="https://demo.foxycart.com/s/admin/subscriptions/0?zoom=last_transaction"
          class="max-w-xl mx-auto"
        >
        </foxy-subscription-form>
      `
    }
  </Story>
</Canvas>
