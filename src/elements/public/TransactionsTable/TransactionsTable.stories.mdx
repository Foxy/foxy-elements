import { html } from 'lit-html';
import { Story, Canvas, Meta } from '@storybook/addon-docs/blocks';
import './index.ts';

<Meta title="Tables / Transactions" component="foxy-transactions-table" />

# Transactions

[![hAPI](https://img.shields.io/badge/hAPI-transactions-green.svg)](https://api.foxycart.com/rels/transactions)

## States

Below are all the states this component can render.

### Loading

Displayed while loading the resource.

<Canvas>
  <Story name="Loading">
    {() =>
      html`
        <foxy-transactions-table href="https://demo.foxycart.com/s/admin/sleep">
        </foxy-transactions-table>
      `
    }
  </Story>
</Canvas>

### Error

Displayed when resource load fails.

<Canvas>
  <Story name="Error">
    {() =>
      html`
        <foxy-transactions-table href="https://demo.foxycart.com/s/admin/not-found">
        </foxy-transactions-table>
      `
    }
  </Story>
</Canvas>

### Ready

Displayed after the resource is loaded.

<Canvas>
  <Story name="Ready">
    {() =>
      html`
        <foxy-transactions-table
          href="https://demo.foxycart.com/s/admin/stores/0/transactions?customer_id=0&limit=10&zoom=items"
        >
        </foxy-transactions-table>
      `
    }
  </Story>
</Canvas>
