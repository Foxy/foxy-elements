import { html } from 'lit-html';
import { Story, Canvas, Meta } from '@storybook/addon-docs/blocks';
import { transactions } from '../../../mocks/FxTransactions';
import './index.ts';

<Meta title="Tables / Transactions" component="foxy-transactions-table" />

# Transactions

[![hAPI](https://img.shields.io/badge/hAPI-transactions-green.svg)](https://api.foxycart.com/rels/transactions)

## States

Below are all the states this component can render.

### Loading

Displayed while loading the resource.

export function handleRequestWithLoading(evt) {
  evt.detail.handle(() => new Promise(() => {}));
}

<Canvas>
  <Story name="Loading">
    {() =>
      html`
        <foxy-transactions-table
          href="https://api.foxy.test/stores/8/transactions?customer_id=115&limit=10"
          @request=${handleRequestWithLoading}
        >
        </foxy-transactions-table>
      `
    }
  </Story>
</Canvas>

### Error

Displayed when resource load fails.

export function handleRequestWithError(evt) {
  evt.detail.handle(() => Promise.reject());
}

<Canvas>
  <Story name="Error">
    {() =>
      html`
        <foxy-transactions-table
          href="https://api.foxy.test/stores/8/transactions?customer_id=115&limit=10"
          @request=${handleRequestWithError}
        >
        </foxy-transactions-table>
      `
    }
  </Story>
</Canvas>

### Ready

Displayed after the resource is loaded.

export function handleRequestWithSuccess(evt) {
  evt.detail.handle(async () => new Response(JSON.stringify(transactions)));
}

<Canvas>
  <Story name="Ready">
    {() =>
      html`
        <foxy-transactions-table
          href="https://api.foxy.test/stores/8/transactions?customer_id=115&limit=10"
          @request=${handleRequestWithSuccess}
        >
        </foxy-transactions-table>
      `
    }
  </Story>
</Canvas>
