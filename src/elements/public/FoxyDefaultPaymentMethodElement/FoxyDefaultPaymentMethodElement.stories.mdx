import { html } from 'lit-html';
import { Story, Canvas, Meta } from '@storybook/addon-docs/blocks';
import { defaultPaymentMethod } from '../../../mocks/FxDefaultPaymentMethod';
import './index.ts';

<Meta title="Relations/DefaultPaymentMethod" component="foxy-default-payment-method" />

# Default Payment Method

[![hAPI](https://img.shields.io/badge/hAPI-default__payment__method-green.svg)](https://api.foxycart.com/rels/default_payment_method)

## States

Below are all the states this component can render.

### Loading

Displayed while loading the resource.

export function handleRequestWithLoading(evt) {
  evt.detail.handle(() => new Promise(() => {}));
}

<Canvas>
  <Story name="Loading">
    {() =>
      html`
        <foxy-default-payment-method
          class="shadow-xl rounded-lg overflow-hidden mx-auto"
          style="max-width: 320px"
          href="https://api.foxycart.com/customers/8/default_payment_method"
          @request=${handleRequestWithLoading}
        >
        </foxy-default-payment-method>
      `
    }
  </Story>
</Canvas>

### Error

Displayed when resource load fails.

export function handleRequestWithError(evt) {
  evt.detail.handle(() => Promise.reject());
}

<Canvas>
  <Story name="Error">
    {() =>
      html`
        <foxy-default-payment-method
          class="shadow-xl rounded-lg overflow-hidden mx-auto"
          style="max-width: 320px"
          href="https://api.foxycart.com/customers/8/default_payment_method"
          @request=${handleRequestWithError}
        >
        </foxy-default-payment-method>
      `
    }
  </Story>
</Canvas>

### Ready

Displayed after the resource is loaded.

export function handleRequestWithSuccess(evt) {
  evt.detail.handle(async () => new Response(JSON.stringify(defaultPaymentMethod)));
}

<Canvas>
  <Story name="Ready">
    {() =>
      html`
        <foxy-default-payment-method
          class="shadow-xl rounded-lg overflow-hidden mx-auto"
          style="max-width: 320px"
          href="https://api.foxycart.com/customers/8/default_payment_method"
          @request=${handleRequestWithSuccess}
        >
        </foxy-default-payment-method>
      `
    }
  </Story>
</Canvas>
