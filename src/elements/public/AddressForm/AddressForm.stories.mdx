import { html } from 'lit-html';
import { Story, Canvas, Meta } from '@storybook/addon-docs/blocks';
import './index.ts';

<Meta title="Forms / Address" component="foxy-address-form" />

# Customer Address Form

[![hAPI](https://img.shields.io/badge/hAPI-customer__address-green.svg)](https://api.foxycart.com/rels/customer_address)

## States

Below are all the states this component can render.

### Loading

Displayed while loading the resource.

<Canvas>
  <Story name="Loading">
    {() =>
      html`
        <foxy-address-form class="max-w-xl mx-auto" href="https://demo.foxycart.com/s/admin/sleep">
        </foxy-address-form>
      `
    }
  </Story>
</Canvas>

### Error

Displayed when resource load fails.

<Canvas>
  <Story name="Error">
    {() =>
      html`
        <foxy-address-form
          href="https://demo.foxycart.com/s/admin/not-found"
          class="max-w-xl mx-auto"
        >
        </foxy-address-form>
      `
    }
  </Story>
</Canvas>

### Empty

Displayed when no resource is specified.

<Canvas>
  <Story name="Empty">
    {() =>
      html`
        <foxy-address-form
          parent="https://demo.foxycart.com/s/admin/customers/0/addresses"
          class="max-w-xl mx-auto"
        >
        </foxy-address-form>
      `
    }
  </Story>
</Canvas>

### Ready

Displayed after the resource is loaded.

<Canvas>
  <Story name="Ready">
    {() =>
      html`
        <foxy-address-form
          href="https://demo.foxycart.com/s/admin/customer_addresses/0"
          class="max-w-xl mx-auto"
        >
        </foxy-address-form>
      `
    }
  </Story>
</Canvas>
