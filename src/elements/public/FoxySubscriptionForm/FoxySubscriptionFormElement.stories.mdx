import { html } from 'lit-html';
import { Story, Canvas, Meta } from '@storybook/addon-docs/blocks';
import { subscription } from '../../../mocks/FxSubscription';
import './index.ts';

<Meta title="Forms/SubscriptionForm" component="foxy-subscription" />

# Subscription Form

[![hAPI](https://img.shields.io/badge/hAPI-subscription-green.svg)](https://api.foxycart.com/rels/subscription)

## States

Below are all the states this component can render.

### Loading

Displayed while loading the resource.

export function handleRequestWithLoading(evt) {
  evt.detail.handle(() => new Promise(() => {}));
}

<Canvas>
  <Story name="Loading">
    {() =>
      html`
        <foxy-subscription
          href="https://api.foxy.test/subscriptions/413"
          @request=${handleRequestWithLoading}
        >
        </foxy-subscription>
      `
    }
  </Story>
</Canvas>

### Error

Displayed when resource load fails.

export function handleRequestWithError(evt) {
  evt.detail.handle(() => Promise.reject());
}

<Canvas>
  <Story name="Error">
    {() =>
      html`
        <foxy-subscription
          href="https://api.foxy.test/subscriptions/413"
          @request=${handleRequestWithError}
        >
        </foxy-subscription>
      `
    }
  </Story>
</Canvas>

### Ready

Displayed after the resource is loaded.

export function handleRequestWithSuccess(evt) {
  evt.detail.handle(async () => new Response(JSON.stringify(subscription)));
}

<Canvas>
  <Story name="Ready">
    {() =>
      html`
        <foxy-subscription
          href="https://api.foxy.test/subscriptions/413"
          @request=${handleRequestWithSuccess}
        >
        </foxy-subscription>
      `
    }
  </Story>
</Canvas>
